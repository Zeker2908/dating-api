spring:
  application:
    name: authentication-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
logging:
  level:
    root: INFO
    org.springframework.security: DEBUG
    eureka: INFO
    ru.zeker: DEBUGz
app:
  security:
    password-history:
      max-count: 5